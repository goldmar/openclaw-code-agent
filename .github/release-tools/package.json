{
  "name": "oca-release-tools",
  "private": true,
  "description": "Pinned ClawHub CLI for the release workflow, installed with `npm ci --ignore-scripts`. Bump clawhub deliberately, then regenerate package-lock.json with `npm install --package-lock-only --ignore-scripts --before=<a date 3 days ago>`.",
  "license": "MIT",
  "dependencies": {
    "clawhub": "0.23.3"
  }
}
